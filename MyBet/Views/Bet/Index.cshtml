@model SocialBetting.Models.MyBetModel 

@{
    ViewBag.Title = "Ставка";
}

<h2>Ставка</h2>

<div>
    <h4>Movie</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>

<p>Возможные варианты</p>
<div>
    @if (ViewBag.lstInfOutcomes != null)
    {
        foreach (var betInf in ViewBag.lstInfOutcomes)
        {
            <br />
            <a href=@Url.Action("Index", "Outcome", new { id = @betInf.Split('|')[0] })> @betInf.Split('|')[1] </a>
        }
    }
</div>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = @Model.id }) 
    @Html.ActionLink("Back to List", "Index","Home")
</p>
